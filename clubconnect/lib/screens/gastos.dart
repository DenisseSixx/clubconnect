import 'package:flutter/material.dart';

class gastos extends StatelessWidget {
  const gastos ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar( title: Text('Consumos y gastos'),),
    );
  }
}